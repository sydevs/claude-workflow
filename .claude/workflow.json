{
  "$comment": "Per-repo values read by the sydevs `workflow` plugin and its skills. Values only — the rules that consume them live in the skills, the stories in docs/why.md. This repo is unusual: it holds the plugin itself, so a change here changes how every other repo is worked on.",
  "packageManager": "none",
  "leanGate": {
    "command": "claude plugin validate ./workflow --strict",
    "contents": "Plugin manifest + skill frontmatter validation. There is no test suite — the skills are prose, and their real gate is a supervised loop run."
  },
  "securityReview": {
    "triggerPattern": "^workflow/hooks/|^workflow/skills/[^/]+/[^/]+\\.md$|^loop-config\\.json$",
    "rationale": "Hooks run in a maintainer's session with their credentials, and every .md in a skill directory is an instruction to an agent holding write access to five repos."
  },
  "worktreeSetup": [],
  "deployTarget": "Consumed live from `main` by the Claude routines and by `/plugin install workflow@sydevs`. There is no release step: merging to main IS the deploy, and the next routine run picks it up.",
  "devServer": null,
  "generatedFiles": [],
  "prAllowlistGlobs": [
    "**"
  ]
}
