{
  "$comment": "VALUES the loop reads, nothing else. A tuning change should be a data edit reviewable on its own, so this file holds numbers, names and ids — never rules and never rationale. The rule lives in the skill that enforces it; the story lives in docs/why.md. Read fresh from main on every routine run, so merging a change here changes the next run with no redeploy.",
  "repos": [
    "SahajCloud",
    "SahajAtlasWeb",
    "WeMeditateWeb",
    "SahajAtlasWordpress",
    "claude-workflow"
  ],
  "org": "sydevs",
  "journalRepo": "claude-workflow",
  "ceilings": {
    "$comment": "Stock caps and per-run backstops. Never a per-run flow quota: at 8 runs/day a 'per run' allowance is 8x a daily one. Rules in loop-run/SKILL.md.",
    "wipCapPerRepo": 3,
    "maxProposalsPerSurvey": 3,
    "maxOpenProposals": 10,
    "ciFixIterations": 3,
    "ciPollAttempts": 20,
    "maxWorkItemsPerRun": 3
  },
  "labels": {
    "$comment": "Only the assignee field and readyToImplement mean anything to the loop; the rest are for humans. Gate rules in triage-issue/SKILL.md.",
    "readyToImplement": "ready-to-implement",
    "proposal": "proposal",
    "hold": "hold",
    "needsInfo": "needs-info",
    "blockedUpstream": "blocked-upstream",
    "journal": "ops-journal",
    "claim": "claude:working"
  },
  "assignment": {
    "$comment": "The baton: assignee:<bot> IS the worklist. Hand-back rules in loop-run/SKILL.md.",
    "bot": "sydevs-bot",
    "reviewer": "Ardnived"
  },
  "mergePolicy": {
    "$comment": "Repos where the loop may open and revise PRs but must never merge them, whatever the gate says. claude-workflow ships the instructions every run is executing, so merging there is the deploy and belongs to a human — and now that the repo is ticketless, review is the ONLY gate its changes pass.",
    "loopMayNotMerge": [
      "claude-workflow"
    ]
  },
  "surveyCalendar": {
    "$comment": "Weekday -> survey skill, morning runs only. Weekends run no surveys. Keys are lowercase UTC weekday names as produced by `date -u +%A | tr A-Z a-z`.",
    "monday": "survey-deps",
    "tuesday": "survey-sentry",
    "wednesday": "survey-analysis",
    "thursday": "survey-contracts",
    "friday": "cut-release",
    "saturday": null,
    "sunday": "reflect"
  },
  "analysisAngles": [
    "correctness and edge cases",
    "simplicity and abstraction reduction",
    "performance",
    "security",
    "test quality and coverage gaps",
    "accessibility",
    "i18n completeness",
    "error handling and observability",
    "UX consistency",
    "feature research against current best practice"
  ],
  "sentry": {
    "$comment": "DE region: the global sentry.io host 404s for these projects. Token has no org:read, so /organizations/ 403s — expected. SahajAtlasWordpress ships no Sentry.",
    "apiBase": "https://de.sentry.io/api/0",
    "org": "sy-developers",
    "projects": {
      "SahajCloud": "sahajcloud",
      "SahajAtlasWeb": "sahajatlas-web",
      "WeMeditateWeb": "wemeditate-web"
    },
    "tokenEnvVar": "SENTRY_CLAUDE_WORKFLOW_TOKEN"
  },
  "mailpit": {
    "$comment": "Basic-auth credentials live in the cloud environment as MAILPIT_UI_AUTH, never here.",
    "url": "https://mailpit-production-db88.up.railway.app",
    "retentionDays": 7
  },
  "issueFields": {
    "$comment": "GitHub's native org-level issue fields — not Projects v2, not labels. Read/write rules and the PUT-replaces-everything gotcha are in triage-issue/SKILL.md.",
    "priority": {
      "fieldId": 14337938,
      "options": [
        "Critical",
        "High",
        "Medium",
        "Low"
      ]
    },
    "effort": {
      "fieldId": 14337941,
      "options": [
        "Easy",
        "Moderate",
        "Hard"
      ]
    }
  },
  "relationships": {
    "$comment": "No MCP tool reads blocked_by, so a cloud run reads this marker from the issue body instead. Set the real relationship locally AND mirror it here.",
    "bodyMarker": "Blocked by:",
    "bodyMarkerFormat": "Blocked by: https://github.com/<org>/<repo>/issues/<n> — <one-line reason>",
    "cloudReadable": false,
    "recheckProbe": "select:mcp__github__issue_dependency_read — if this ever resolves, switch loop-run to the tool and retire the body-marker fallback"
  },
  "identity": {
    "$comment": "expectedLogin is authoritative and is checked against get_me once per run. Attribution rules in loop-run/SKILL.md.",
    "expectedLogin": "sydevs-bot",
    "commentMarker": "<sub>🤖 Written by the sydevs autonomous loop — see [what this is](https://github.com/sydevs/claude-workflow#the-loop)</sub>"
  },
  "review": {
    "$comment": "Rung 5 / adversarial-review. Advisory only. Rules in adversarial-review/SKILL.md.",
    "bodyHeader": "## 🧐 Adversarial review",
    "profilePath": "workflow/skills/adversarial-review/reviewer-profile.md"
  },
  "journal": {
    "$comment": "One issue per day in journalRepo, created lazily. No issue number here on purpose: the run finds today's by creation date.",
    "titleFormat": "<Day> — <n> new, <n> revised, <n> merged, <n> closed",
    "timezone": "America/Vancouver"
  }
}
